import { createMuiTheme } from '@material-ui/core/styles';

export const createTheme = (theme) => createMuiTheme({
    palette: {
        primary: {
            light: '#4fb3bf',
            main: '#00838f',
            dark: '#005662',
            contrastText: '#fff',
        },
        secondary: {
            light: '#4fb3bf',
            main: '#00838f',
            dark: '#005662',
            contrastText: '#fff',
        },
    },
});
